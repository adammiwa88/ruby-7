= @user.name
= ','
= @user.email

%br
= @user.created_at
%br
= @user.updated_at
%br
= Time.now

- provide(:title, @user.name)
.row
  %aside.col-md-4
    %section.user_info
      %h1
        = gravatar_for @user
        = @user.name